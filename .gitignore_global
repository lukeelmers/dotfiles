# This file includes system-wide global ignores for items that always should be ignored.
# Add it using git config --global core.excludesfile ~/.gitignore_global
# 
# See http://help.github.com/ignore-files/ for more about ignoring files.

.DS_Store
